# Server-layer dependencies only.
# torch / spconv / flash-attn / pointops / timm / albumentations etc. are
# provided by the base image layers (see Dockerfile).
fastapi>=0.115
uvicorn[standard]>=0.30
huggingface_hub>=0.30
python-multipart>=0.0.18
requests>=2.32
